import fs from 'node:fs';
const status=JSON.parse(fs.readFileSync('docs/stages/status.json','utf8'));
const native=status.validation_latest,candidate=status.candidate_validation,budget=status.paid_inference;
const frontend=status.frontend_workspace;
const current=frontend?.status==='deployed_native_verified'?frontend:null;
const qualification=current
  ? `Native release **${current.version}** is recorded as deployed and verified in its [native receipt](${current.native_receipt}). The full regression baseline at **${current.full_suite_baseline.version}** passed ${current.full_suite_baseline.tests.toLocaleString('en-US')} tests across ${current.full_suite_baseline.files} files. Release **${current.version}** has ${current.native_checks} native checks plus typecheck, build and the focused loading-provenance browser check; the full suite was not rerun for its loading-label-only change. [Current presenter route](${current.presenter_route}). Automatic platform browser SSO handoff and human instructor acceptance remain open. Current evidence and remaining acceptance are in [stage status](docs/stages/status.json) and the [build journal](docs/process/BUILD-JOURNAL.md). Automated checks and scripted demonstrations do not establish human learning efficacy.`
  : `Native release **${native.native_version}** has ${native.tests.toLocaleString('en-US')} automated tests across ${native.test_files} files recorded in its release evidence. ${candidate?`Local candidate **${candidate.version}** has ${candidate.tests.toLocaleString('en-US')} passing tests across ${candidate.test_files} files; independent native review/deployment is ${candidate.native_release}. `:''}Current evidence and remaining acceptance are in [stage status](docs/stages/status.json) and the [build journal](docs/process/BUILD-JOURNAL.md). Automated checks and scripted demonstrations do not establish human learning efficacy.`;
const spend=`The recorded native ledger is **${budget.reported_requests}/${budget.request_cap} requests** and **USD ${budget.conservative_cost_usd}/${budget.usd_cap} committed**. ${budget.request_cap-budget.reported_requests} requests remain; no generation is required for the prepared presentation.`;
let text=fs.readFileSync('README.md','utf8');text=text.replace(/<!-- qualification:start -->[\s\S]*?<!-- qualification:end -->/,`<!-- qualification:start -->\n${qualification}\n<!-- qualification:end -->`);text=text.replace(/<!-- inference-status:start -->[\s\S]*?<!-- inference-status:end -->/,`<!-- inference-status:start -->\n${spend}\n<!-- inference-status:end -->`);fs.writeFileSync('README.md',text);
